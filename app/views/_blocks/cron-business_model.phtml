<?php if (allow_acl('businessModel', App\Controllers\CronController::class)) : ?>
    <?php if ($block??0) : ?>
    <div class="col-md-3">
        <div class="form-group">
            <a href="javascript:;" id="business_model" class="btn blue btn-block" data-style="zoom-in">
                <span class="ladda-label"><i class="fa fa-database"></i> <?=config('title.action.businessModel')?></span>
                <span class="ladda-spinner"></span>
            </a>
        </div>
    </div>
    <?php else: ?>
        <a href="javascript:;" id="business_model" class="btn blue" data-style="zoom-in">
            <span class="ladda-label"><i class="fa fa-database"></i> <?=config('title.action.businessModel')?></span>
            <span class="ladda-spinner"></span>
        </a>
    <?php endif; ?>
    <script>
    jQuery(document).ready(function () {
        $('#business_model').click(function () {
            var l = Ladda.create(document.querySelector('#business_model'));
            l.start();
            $.get(SUB_DOMAIN+'/cron/business-model', function (d) {
                const success = parseInt(d.data.success);
                const count   = parseInt(d.data.count);
                
                swal(`Xử lý thành công : ${success.toLocaleString('vi-VN')} , từ ${count.toLocaleString('vi-VN')} dữ liệu`, {
                    icon: 'success'
                });
                
                l.stop();
            });
        });
    });    
    </script>
<?php endif; ?>